//import { BATCHER } from '../../userTests/apiTests/start2.ts'

self.onmessage = (e) => {
	//const { filename } = e.data;
	//const text = await Deno.readTextFile(filename);
	console.log("test");
	return "returned test"
};